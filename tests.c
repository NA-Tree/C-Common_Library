#include <stdio.h>
#include "functions.h"

int main(int argc, char* argv[])
{


    int a = 32;
    int b = 128;
    //the greatest common divisor for these two numbers is 8
    int result = gcd(a, b);
    printf("%d", result);




























    return 0;
}